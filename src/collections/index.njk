---
layout: base.njk
title: Collections
---

<section class="collections-page">
  <header class="page-header">
    <h1>Photo Collections</h1>
    <p class="intro-text">Moments grouped by mood, session, or silence. Some are open. Some are waiting for you.</p>
  </header>

  <div class="collections-grid">
    {% for collection in collections %}
      <a href="/collections/{{ collection.slug }}/" class="collection-card">
        <div class="thumbnail-wrapper">
          {% if collection.access_level == "public" or user.access_level >= collection.access_level %}
            <img src="{{ collection.cover_image_url }}" alt="{{ collection.title }}">
          {% else %}
            <div class="blurred-thumbnail">
              <img src="{{ collection.cover_image_url }}" alt="{{ collection.title }}">
              <div class="overlay">
                <span class="access-label">
                  {{ collection.access_level | capitalize }} Only
                </span>
              </div>
            </div>
          {% endif %}
        </div>

        <div class="collection-info">
          <h2>{{ collection.title }}</h2>
          <p class="subtitle">{{ collection.subtitle }}</p>
          <p class="photo-count">{{ collection.photo_count }} photos</p>
        </div>
      </a>
    {% endfor %}
  </div>
</section>
