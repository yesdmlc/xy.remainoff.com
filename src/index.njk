---
layout: base.njk
title: Home
---

<header class="hero">
  <img
    src="https://images.unsplash.com/photo-1563688139-bb5832571d4b?q=80&w=2340&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D"
    alt="Hero Image" />
  <div class="overlay">
    <h1 class="hero-title">Lonely doesn’t mean lifeless.</h1>
    <p class="hero-subtitle">This is my quiet—documented in motion, muscle, and memory.</p>
  </div>
</header>

<main>
  <section class="home-collections">
    <h2>Latest Collections</h2>
    <ul class="collection-list">
      {% for c in signedPhotoCollections %}
      <li>
        <a href="/collections/{{ c.slug }}/">
          <img src="{{ c.cover_image_url }}" alt="{{ c.title }}" class="collection-thumb" loading="lazy">
          <span>{{ c.title }}</span>
        </a>
      </li>
      {% endfor %}
    </ul>
    {% for c in signedPhotoCollections %}
      {{ loop.index }} - {{ c.slug }}<br>
    {% endfor %}
  </section>

  <section class="home-latest-post">
    <h2>Latest Posts</h2>
    <div class="post-grid">
      {% for post in supabasePosts %}
        {% if post.data.access_level == "public" and post.data.images.length > 0 %}
        <article class="post-entry">
          <img src="{{ post.data.images[0] }}" alt="{{ post.data.title }}" class="post-thumb" loading="lazy">
          <p>{{ post.data.title }}</p>
        </article>
        {% endif %}
      {% endfor %}
    </div>
  </section>
</main>

<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.js" defer></script>
<script src="/assets/auth.js" defer></script>
<script>
  document.addEventListener("DOMContentLoaded", () => {
    const images = document.querySelectorAll("img");

    images.forEach(img => {
      img.addEventListener("error", () => {
        console.warn("🚫 Image failed to load:", img.src);
      });

      img.addEventListener("load", () => {
        console.log("✅ Image loaded:", img.src);
      });
    });
  });
</script>