---
layout: base.njk
title: All Posts
eleventyExcludeFromCollections: true
permalink: "/posts/"
---

<h1>All Posts</h1>

<section class="post-feed">
  {% for post in publicSupabasePosts %}
  {% set isFirstThree = loop.index0 < 3 %}
  <article class="post-entry">
    <a href="{{ post.url }}" class="post-link">
      <div class="post-card">
        <div class="post-thumb-wrapper">
          <img
            src="{{ post.signed_cover_image_url }}"
            alt="{{ post.alt or post.title }}"
            class="post-cover"
            {% if not isFirstThree %}loading="lazy"{% endif %}
          />
        </div>
        <div class="post-caption">
          <p>{{ post.caption }}</p>
          <small>
            {% if post.date and (post.date | date('yyyy')) != 'Invalid DateTime' %}
              {{ post.date | date("MMM d, yyyy") }}{% if post.mood %} · {{ post.mood }}{% endif %}
            {% else %}
              No date
            {% endif %}
          </small>
        </div>
      </div>
    </a>
  </article>
  {% endfor %}
</section>

<script>
  window.SUPABASE_CONFIG = JSON.parse('{{ "" | supabaseConfig | safe }}');
</script>
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.js" defer></script>
<script src="/assets/auth.js" defer></script>